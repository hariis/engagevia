 <% if @engagement.notify? %>
    <%= link_to_remote 'Disable Notification', :url => {:controller => 'engagements', :action => 'set_notification', :id => @engagement.id, :set => 'false'} %>
 <% else %>
    <%= link_to_remote 'Enable Notification', :url => {:controller => 'engagements', :action => 'set_notification', :id => @engagement.id, :set => 'true'} %>
 <% end %>